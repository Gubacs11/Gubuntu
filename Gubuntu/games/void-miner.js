"use strict";

/* VOID MINER is deliberately self-contained, but uses Gubuntu's shared save,
   reward, audio, pause, and normalized mobile-input systems. */
function startVoidMiner(options={}){
  const vm=currentPlayer.voidMiner;
  const resourceDefs={
    iron:{name:"IRON",color:"#a9b4c2",value:12,min:0,max:260,hardness:1,weight:8,desc:"Reliable, honest, and not whispering."},
    copper:{name:"COPPER",color:"#f08a4b",value:16,min:0,max:320,hardness:1,weight:7,desc:"Conducts electricity and bad plans."},
    silver:{name:"SILVER",color:"#d7edff",value:34,min:90,max:460,hardness:2,weight:5,desc:"The mine's fanciest tableware."},
    gold:{name:"GOLD",color:"#ffe04b",value:62,min:130,max:600,hardness:2,weight:4,desc:"Heavy optimism in nugget form."},
    crystal:{name:"CRYSTAL",color:"#31f5ff",value:95,min:240,max:720,hardness:2,weight:3,desc:"Glows without permission."},
    uranium:{name:"URANIUM",color:"#72ff77",value:145,min:430,max:900,hardness:3,weight:2.2,desc:"Warm. Too warm. Stop holding it."},
    voidstone:{name:"VOIDSTONE",color:"#c35cff",value:270,min:690,max:9999,hardness:4,weight:1.8,desc:"Do not stare at it for too long."},
    prism:{name:"PRISM SHARD",color:"#ff7df4",value:430,min:260,max:9999,hardness:2,weight:.38,rare:true,desc:"Contains seven colors and one legal warning."},
    core:{name:"ANCIENT CORE",color:"#ffb547",value:620,min:420,max:9999,hardness:3,weight:.25,rare:true,desc:"Still running. Nobody found the off switch."},
    null:{name:"NULL CRYSTAL",color:"#f3e7ff",value:850,min:710,max:9999,hardness:4,weight:.18,rare:true,desc:"Inventory count occasionally denies it exists."},
    alloy:{name:"ALIEN ALLOY",color:"#6effd7",value:720,min:560,max:9999,hardness:3,weight:.2,rare:true,desc:"Made elsewhere. Very elsewhere."},
    pearl:{name:"VOID PEARL",color:"#ffffff",value:1200,min:760,max:9999,hardness:4,weight:.1,rare:true,desc:"A tiny moon from an ocean that never happened."}
  };
  const artifacts={
    starmap:{name:"BROKEN STAR MAP",icon:"✦",desc:"Every route points down."},engine:{name:"NULL ENGINE CORE",icon:"◉",desc:"Produces negative noise."},
    helmet:{name:"ANCIENT MINER HELMET",icon:"⛑",desc:"The lamp turns on near things that are not there."},eye:{name:"VOID EYE",icon:"◍",desc:"It is filed under collectible. It disagrees."},
    heart:{name:"CRYSTAL HEART",icon:"◆",desc:"Beats exactly once per save."},battery:{name:"ALIEN BATTERY",icon:"ϟ",desc:"Warranty valid in three extinct empires."}
  };
  const upgrades={
    drill:{name:"DRILL",icon:"⛏",desc:"Mine faster and cut harder terrain.",cost:[180,520,1200,2700],levels:["MK.I","MK.II","PLASMA","QUANTUM","VOID CUTTER"]},
    battery:{name:"BATTERY",icon:"▰",desc:"More power for longer descents.",cost:[140,380,850,1900]},cargo:{name:"CARGO",icon:"▣",desc:"Carry more material.",cost:[120,350,800,1750]},
    suit:{name:"SUIT",icon:"⬡",desc:"More health and hazard resistance.",cost:[160,430,980,2100]},light:{name:"LIGHT",icon:"☀",desc:"Wider headlamp radius.",cost:[100,280,650,1450]},
    scanner:{name:"SCANNER",icon:"⌁",desc:"Rare deposits pulse through rock.",cost:[210,540,1250,2600]},mobility:{name:"MOBILITY",icon:"»",desc:"Faster movement and shorter dash cooldown.",cost:[130,360,820,1800]}
  };
  let cleanup=()=>{};
  setActiveCleanup(()=>cleanup());
  const discover=(type,id)=>{const list=vm.discoveries[type];if(!list.includes(id)){list.push(id);toast(`CODEX UPDATED • ${(resourceDefs[id]||artifacts[id])?.name||id.toUpperCase()}`);}};
  const statCards=()=>`<div class="vm-records"><span>DEEPEST<b>${vm.stats.deepestDepth||0} m</b></span><span>EXTRACTIONS<b>${vm.stats.extractions||0}/${vm.stats.runs||0}</b></span><span>VALUE BANKED<b>${(vm.stats.totalValue||0).toLocaleString()}</b></span><span>ARTIFACTS<b>${vm.stats.artifactsFound||0}</b></span></div>`;
  const bindMenu=()=>{$("#vm-start")?.addEventListener("click",startRun);$("#vm-upgrades")?.addEventListener("click",renderUpgrades);$("#vm-codex")?.addEventListener("click",renderCodex);};
  function renderTitle(){cleanup();cleanup=()=>{};setStage(`<section class="vm-title"><div class="vm-title-mark">⛏</div><p class="eyebrow">GUBUNTU DEEP EXTRACTION DIVISION</p><h3>VOID MINER</h3><b>DESCEND. EXTRACT. RETURN.</b><p>The company has reviewed the darkness. The darkness is now billable.</p>${statCards()}<div class="game-actions"><button id="vm-start" class="pixel-btn primary">START RUN</button><button id="vm-upgrades" class="pixel-btn secondary">UPGRADES</button><button id="vm-codex" class="pixel-btn secondary">CODEX</button></div><small>${vm.credits.toLocaleString()} CREDITS • DRILL ${upgrades.drill.levels[vm.upgrades.drill]} • DAILY AND RANDOM SEEDS</small></section>`);bindMenu();}
  function renderUpgrades(){setStage(`<section class="vm-base"><header><div><p class="eyebrow">SURFACE BASE</p><h3>EQUIPMENT BAY</h3></div><strong>${vm.credits.toLocaleString()} CR</strong></header><div class="vm-upgrade-grid">${Object.entries(upgrades).map(([id,u])=>{const level=vm.upgrades[id],max=level>=4,cost=u.cost[level];return `<article><i>${u.icon}</i><div><b>${u.name} • ${id==="drill"?u.levels[level]:`LV.${level+1}`}</b><p>${u.desc}</p><small>${max?"MAXIMUM CALIBRATION":`NEXT LEVEL • ${cost} CR`}</small></div><button data-vm-upgrade="${id}" ${max||vm.credits<cost?"disabled":""}>${max?"MAX":"BUY"}</button></article>`}).join("")}</div><button id="vm-back" class="pixel-btn secondary">BACK TO TERMINAL</button></section>`);$$('[data-vm-upgrade]').forEach(button=>button.onclick=()=>{const id=button.dataset.vmUpgrade,u=upgrades[id],level=vm.upgrades[id],cost=u.cost[level];if(level>=4||vm.credits<cost)return;vm.credits-=cost;vm.upgrades[id]++;saveData();sfx("buy");renderUpgrades()});$("#vm-back").onclick=renderTitle;}
  function renderCodex(){const categories=[
    ["resources","RESOURCES",resourceDefs],
    ["artifacts","ARTIFACTS",artifacts],
    ["zones","DEPTH ZONES",{upper:{name:"UPPER MINE",desc:"0–100 m • Dirt, stone, and manageable paperwork."},deep:{name:"DEEP ROCK",desc:"100–250 m • Gas, silver, gold."},crystal:{name:"CRYSTAL DEPTHS",desc:"250–450 m • Electric geology."},obsidian:{name:"OBSIDIAN LAYER",desc:"450–700 m • Heat, lava, uranium."},void:{name:"THE VOID",desc:"700+ m • The mine stops obeying nouns."}}],
    ["rooms","SPECIAL ROOMS",{camp:{name:"ABANDONED MINER CAMP",desc:"Emergency supplies, abandoned optimism."},vault:{name:"CRYSTAL VAULT",desc:"A chamber with excellent loot and terrible acoustics."},lab:{name:"ANCIENT LAB",desc:"Research continued after the researchers did not."},shaft:{name:"COLLAPSED SHAFT",desc:"A risky shortcut made mostly of falling."},shrine:{name:"VOID SHRINE",desc:"Trades power for consequences."},drill:{name:"LOST DRILL",desc:"Temporary drill overclock."}}],
    ["creatures","CREATURES",{crawler:{name:"CAVE CRAWLER",desc:"Likes ankles and unsecured minerals."},mite:{name:"CRYSTAL MITE",desc:"Small, bright, electrically rude."},wisp:{name:"VOID WISP",desc:"A thought the cave forgot to stop thinking."},stalker:{name:"NULL STALKER",desc:"It follows from just beyond the lamp."}}],
    ["logs","MINER LOGS",{log14:{name:"MINER LOG 14",desc:"Something keeps knocking from the other side of the wall."},log27:{name:"MINER LOG 27",desc:"We stopped calling it rock three days ago."},log41:{name:"MINER LOG 41",desc:"Do not follow the purple lights."},log63:{name:"MINER LOG 63",desc:"The elevator returned before we called it."}}]
  ];setStage(`<section class="vm-codex"><header><div><p class="eyebrow">DISCOVERY LOG</p><h3>VOID CODEX</h3></div><strong>${categories.reduce((n,[type])=>n+vm.discoveries[type].length,0)} DISCOVERIES</strong></header>${categories.map(([type,label,defs])=>`<div><h4>${label} • ${vm.discoveries[type].length}/${Object.keys(defs).length}</h4><section>${Object.entries(defs).map(([id,d])=>{const found=vm.discoveries[type].includes(id);return `<article class="${found?"found":"unknown"}"><i>${found?(d.icon||"◆"):"?"}</i><b>${found?d.name:"UNKNOWN"}</b><p>${found?d.desc:"NO VERIFIED DATA"}</p></article>`}).join("")}</section></div>`).join("")}<button id="vm-back" class="pixel-btn secondary">BACK TO TERMINAL</button></section>`);$("#vm-back").onclick=renderTitle;}

  function startRun(){
    cleanup();
    const W=900,H=570,T=30,COLS=46,ROWS=104,spawnX=Math.floor(COLS/2),spawnY=3,hard=options.difficulty==="hard",daily=options.mode==="daily";
    const seed=daily?Number(new Date().toISOString().slice(0,10).replaceAll("-","")):Math.floor(Math.random()*0xffffffff),seedLabel=(seed>>>0).toString(16).toUpperCase().padStart(8,"0").match(/.{1,4}/g).join("-");
    let state=seed>>>0||0x6d2b79f5;const rand=()=>{state+=0x6D2B79F5;let t=state;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296};
    const cells=Array.from({length:ROWS},()=>Array(COLS)),objects=[],enemies=[],particles=[],clouds=[],floaters=[],projectiles=[],decorations=[],caverns=[],ambientMotes=[],breakBursts=[],keys=new Set(),mouse={x:W/2,y:H/2,down:false};
    const discovered=Array.from({length:ROWS},()=>new Uint8Array(COLS)),cargo={},runArtifacts=[];let cargoCount=0,blocks=0,maxDepth=0,ended=false,raf=0,last=performance.now(),lastMapDraw=0,eventClock=24+rand()*18,eventText="",eventLife=0,shake=0,mineCell=null,mineProgress=0,mineSoundClock=0,drillRecoil=0,dashCooldown=0,temporaryDrill=0,extractionPrompt=false,powerWarning=0;
    const maxPower=125+vm.upgrades.battery*35,maxHp=100+vm.upgrades.suit*22,cargoMax=28+vm.upgrades.cargo*9,drillTier=1+vm.upgrades.drill;
    const player={x:(spawnX+.5)*T,y:(spawnY+.5)*T,r:9,hp:maxHp,power:maxPower,angle:Math.PI/2,invuln:0,dash:0};
    const zones=[{id:"upper",name:"UPPER MINE",min:0,color:"#76614f",bg:"#100d16"},{id:"deep",name:"DEEP ROCK",min:100,color:"#596275",bg:"#090d18"},{id:"crystal",name:"CRYSTAL DEPTHS",min:250,color:"#27677a",bg:"#03121d"},{id:"obsidian",name:"OBSIDIAN LAYER",min:450,color:"#632d2b",bg:"#180707"},{id:"void",name:"THE VOID",min:700,color:"#402258",bg:"#08030e"}];
    const zoneAt=d=>[...zones].reverse().find(z=>d>=z.min)||zones[0],depthAtY=y=>Math.max(0,Math.floor((y/T-spawnY-.5)*10)),cellDepth=y=>Math.max(0,(y-spawnY)*10);
    const materials={dirt:{name:"DIRT",hard:1,hp:16,color:"#695343"},stone:{name:"STONE",hard:1,hp:24,color:"#48505d"},hard:{name:"HARD ROCK",hard:2,hp:37,color:"#353b49"},crystal:{name:"CRYSTAL ROCK",hard:2,hp:43,color:"#174d60"},obsidian:{name:"OBSIDIAN",hard:3,hp:58,color:"#261e2b"},void:{name:"VOIDSTONE SHELL",hard:4,hp:76,color:"#25152f"}};
    const materialFor=d=>d<80?(rand()<.48?"dirt":"stone"):d<250?(rand()<.32?"hard":"stone"):d<450?(rand()<.38?"crystal":"hard"):d<700?(rand()<.42?"obsidian":"hard"):(rand()<.55?"void":"obsidian");
    const chooseResource=d=>{const eligible=Object.entries(resourceDefs).filter(([,r])=>d>=r.min&&d<=r.max&&r.hardness<=Math.min(4,1+Math.floor(d/180))),total=eligible.reduce((n,[,r])=>n+r.weight,0);let roll=rand()*total;for(const [id,r] of eligible){roll-=r.weight;if(roll<=0)return id}return null};
    for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++){if(y<=spawnY){cells[y][x]={solid:false};continue}const depth=cellDepth(y),mat=materialFor(depth),m=materials[mat],open=rand()<(y<8?.04:.075);let res=null;if(!open&&rand()<Math.min(.27,.105+depth/5000))res=chooseResource(depth);cells[y][x]={solid:!open,mat,hp:m.hp,maxHp:m.hp,res,hazard:null};if(!open&&depth>105&&rand()<.018)cells[y][x].hazard="gas";if(!open&&depth>220&&depth<570&&rand()<.013)cells[y][x].hazard="electric";}
    const clampCell=(value,max)=>Math.max(2,Math.min(max-3,Math.round(value))),carve=(x,y)=>{const c=cells[y]?.[x];if(c){c.solid=false;c.hazard=null}},carveDisk=(cx,cy,rx=1,ry=rx)=>{for(let y=Math.floor(cy-ry-1);y<=Math.ceil(cy+ry+1);y++)for(let x=Math.floor(cx-rx-1);x<=Math.ceil(cx+rx+1);x++){const wobble=.82+rand()*.32;if(((x-cx)**2/(rx*rx+1)+(y-cy)**2/(ry*ry+1))<=wobble)carve(x,y)}},carveLine=(ax,ay,bx,by,width=1)=>{const steps=Math.max(1,Math.ceil(Math.hypot(bx-ax,by-ay)*2));for(let i=0;i<=steps;i++){const t=i/steps;carveDisk(ax+(bx-ax)*t,ay+(by-ay)*t,width,width)}};
    // One continuous, readable spine guarantees extraction; every other feature connects back to it.
    const mainPath=Array(ROWS).fill(spawnX),branchEnds=[];let pathX=spawnX,previousPathX=spawnX;
    for(let y=spawnY;y<ROWS-2;y++){const zone=zoneAt(cellDepth(y)),style={upper:{drift:.45,width:2},deep:{drift:.68,width:1},crystal:{drift:.5,width:2},obsidian:{drift:.3,width:1},void:{drift:.72,width:1}}[zone.id];previousPathX=pathX;if(zone.id==="void"&&y%6===0)pathX=clampCell(pathX+(rand()<.5?-4:4),COLS);else if(rand()<style.drift)pathX=clampCell(pathX+(rand()<.5?-1:1),COLS);mainPath[y]=pathX;carveLine(previousPathX,Math.max(spawnY,y-1),pathX,y,style.width);carveDisk(pathX,y,style.width,zone.id==="upper"?2:style.width);if(y%13===0)carveDisk(pathX,y,zone.id==="crystal"?4:3,zone.id==="upper"?3:2)}
    // Zone-aware branches: broad upper galleries, tangled deep rock, crystal pockets, shafts, and broken Void geometry.
    for(let i=0;i<62;i++){let y=6+Math.floor(rand()*(ROWS-12)),x=mainPath[y],zone=zoneAt(cellDepth(y)),steps=zone.id==="upper"?14+rand()*18:zone.id==="deep"?18+rand()*25:zone.id==="crystal"?16+rand()*22:zone.id==="obsidian"?13+rand()*20:18+rand()*28,angle=rand()*Math.PI*2;if(zone.id==="obsidian")angle=(rand()<.65?Math.PI/2:0)+(rand()<.5?0:Math.PI);if(zone.id==="void")angle=Math.round(angle/(Math.PI/2))*(Math.PI/2);for(let s=0;s<steps;s++){const width=zone.id==="upper"?1.7:zone.id==="crystal"&&s%8<3?2.4:zone.id==="void"&&s%6===0?2:1;carveDisk(x,y,width,width);if(rand()<.22)angle+=(rand()-.5)*(zone.id==="deep"?1.8:.8);x=clampCell(x+Math.cos(angle),COLS);y=Math.max(5,Math.min(ROWS-4,y+Math.sin(angle)));if(zone.id==="void"&&s%7===0)angle+=Math.PI/2*(rand()<.5?-1:1)}branchEnds.push({x:Math.round(x),y:Math.round(y)});if(i%4===0){const targetY=Math.max(5,Math.min(ROWS-4,Math.round(y+(rand()-.5)*12)));carveLine(x,y,mainPath[targetY],targetY,1)}}
    // Rare landmark caverns create the large visual beats and remain attached to the spine.
    const cavernDefs=[{type:"lake",min:12,max:25},{type:"crystalCavern",min:30,max:48},{type:"industrial",min:17,max:62},{type:"lavaChamber",min:52,max:73},{type:"voidCathedral",min:76,max:98}];
    cavernDefs.forEach((def,index)=>{if(index>1&&rand()>.68)return;const cy=def.min+Math.floor(rand()*(def.max-def.min)),side=rand()<.5?-1:1,cx=clampCell(mainPath[cy]+side*(6+Math.floor(rand()*7)),COLS),rx=5+Math.floor(rand()*3),ry=3+Math.floor(rand()*3);carveLine(mainPath[cy],cy,cx,cy,1);carveDisk(cx,cy,rx,ry);caverns.push({type:def.type,cx,cy,rx,ry});if(def.type==="crystalCavern")for(let n=0;n<7;n++){const x=clampCell(cx-rx+1+rand()*(rx*2-2),COLS),y=Math.max(5,Math.min(ROWS-3,Math.round(cy-ry+1+rand()*(ry*2-2))));const c=cells[y][x];Object.assign(c,{solid:true,mat:"crystal",hp:materials.crystal.hp,maxHp:materials.crystal.hp,res:rand()<.18?"prism":"crystal",hazard:null})}if(def.type==="lavaChamber")for(let x=cx-rx+1;x<=cx+rx-1;x++){const c=cells[cy+ry-1]?.[x];if(c&&!c.solid)c.hazard="lava"}});
    const roomTypes=["camp","vault","lab","shaft","shrine","drill"];
    for(let i=0;i<14;i++){const cy=9+Math.floor(i*(ROWS-17)/13),side=i%2?-1:1,cx=clampCell(mainPath[cy]+side*(4+Math.floor(rand()*8)),COLS),radius=2+Math.floor(rand()*2),type=roomTypes[i%roomTypes.length];carveLine(mainPath[cy],cy,cx,cy,1);carveDisk(cx,cy,radius+1,radius);objects.push({type,x:(cx+.5)*T,y:(cy+.5)*T,used:false,scene:true});if(type==="vault")for(let n=0;n<4;n++)objects.push({type:"loose",res:cellDepth(cy)>500?"voidstone":"crystal",x:(cx+rand()*3-1)*T,y:(cy+rand()*3-1)*T,used:false});}
    // Exploration rewards favor branch ends and dead ends without flooding the mine.
    const logIds=["log14","log27","log41","log63"];branchEnds.filter((_,i)=>i%7===0).forEach((p,i)=>objects.push({type:i%4===0?"log":i%3===0?"battery":"crate",id:logIds[i%logIds.length],x:(p.x+.5)*T,y:(p.y+.5)*T,used:false}));
    for(let y=47;y<ROWS;y++)for(let x=2;x<COLS-2;x++)if(!cells[y][x].solid&&rand()<.007)cells[y][x].hazard="lava";
    const openSpots=[];for(let y=6;y<ROWS-3;y++)for(let x=2;x<COLS-2;x++)if(!cells[y][x].solid){openSpots.push({x,y});const zone=zoneAt(cellDepth(y));if(rand()<.018)decorations.push({x:(x+.5)*T,y:(y+.5)*T,type:zone.id==="upper"?["beam","track","lamp"][Math.floor(rand()*3)]:zone.id==="deep"?["crack","rubble","cable"][Math.floor(rand()*3)]:zone.id==="crystal"?["crystalDust","crystalSpire"][Math.floor(rand()*2)]:zone.id==="obsidian"?["ash","fracture"][Math.floor(rand()*2)]:["glyph","debris","shadow"][Math.floor(rand()*3)]});if(rand()<.012&&ambientMotes.length<150)ambientMotes.push({x:(x+rand())*T,y:(y+rand())*T,phase:rand()*6.28,size:1+rand()*2,zone:zone.id})}
    for(let i=0;i<18;i++){const spot=openSpots[Math.floor(rand()*openSpots.length)],depth=cellDepth(spot.y),type=depth>720?(rand()<.15?"stalker":"wisp"):depth>260?"mite":"crawler",hp=type==="stalker"?95:type==="wisp"?52:36;enemies.push({type,x:(spot.x+.5)*T,y:(spot.y+.5)*T,homeX:(spot.x+.5)*T,homeY:(spot.y+.5)*T,hp,maxHp:hp,r:type==="stalker"?12:9,hit:0,cooldown:1+rand()*3,awake:false,phase:rand()*6.28});}
    const artifactIds=Object.keys(artifacts),artifactSpots=[...branchEnds].sort((a,b)=>b.y-a.y);artifactIds.forEach((id,i)=>{const p=artifactSpots[i*7%artifactSpots.length];objects.push({type:"artifact",id,x:(p.x+.5)*T,y:(p.y+.5)*T,used:false})});
    for(let y=spawnY-1;y<=spawnY+2;y++)for(let x=spawnX-2;x<=spawnX+2;x++)if(cells[y]?.[x])cells[y][x].solid=false;
    objects.push({type:"elevator",kind:"surface",label:"SURFACE ELEVATOR",x:(spawnX+.5)*T,y:(spawnY+.5)*T,used:false});const emergencyUsed=new Set();[48,82].forEach((targetRow,index)=>{const spot=branchEnds.filter(p=>!emergencyUsed.has(p)).sort((a,b)=>Math.abs(a.y-targetRow)-Math.abs(b.y-targetRow))[0];if(!spot)return;emergencyUsed.add(spot);objects.push({type:"elevator",kind:"emergency",label:`EMERGENCY LIFT ${index+1}`,x:(spot.x+.5)*T,y:(spot.y+.5)*T,used:false})});vm.stats.runs++;saveData();
    setStage(`<section class="vm-run"><header class="vm-hud"><span>DEPTH<b id="vm-depth">0 m</b><small id="vm-zone">UPPER MINE</small></span><span>HP<b id="vm-hp">${maxHp}/${maxHp}</b><i><u id="vm-hp-fill"></u></i></span><span>POWER<b id="vm-power">${maxPower}/${maxPower}</b><i><u id="vm-power-fill"></u></i></span><span>CARGO<b id="vm-cargo">0/${cargoMax}</b><i><u id="vm-cargo-fill"></u></i></span><span>TOOL<b id="vm-tool">${upgrades.drill.levels[vm.upgrades.drill]}</b><small>SEED ${seedLabel}</small></span></header><div class="vm-canvas-wrap"><canvas id="vm-canvas" width="${W}" height="${H}" aria-label="Procedural VOID MINER cave"></canvas><aside class="vm-navigation" aria-label="Mine navigation"><canvas id="vm-minimap" width="138" height="116" aria-label="Discovered mine minimap"></canvas><b id="vm-exit-readout">EXIT • 0 m</b></aside><div id="vm-event" class="vm-event"></div><div id="vm-prompt" class="vm-prompt"></div></div><footer><div id="vm-inventory" class="vm-inventory"><small>CARGO EMPTY</small></div><p>WASD MOVE • MOUSE AIM • HOLD LMB/SPACE MINE • E USE/EXTRACT • SHIFT DASH</p></footer></section>`);
    const canvas=$("#vm-canvas"),ctx=canvas.getContext("2d",{alpha:false}),mapCanvas=$("#vm-minimap"),mapCtx=mapCanvas.getContext("2d");canvas.dataset.openCells=String(openSpots.length);canvas.dataset.branches=String(branchEnds.length);canvas.dataset.caverns=String(caverns.length);let exitCount=0;for(let i=0;i<objects.length;i++)if(objects[i].type==="elevator")exitCount++;canvas.dataset.exits=String(exitCount);canvas.dataset.connectedSpine="true";canvas.dataset.renderer="chunked-v2";
    const hud={depth:$("#vm-depth"),zone:$("#vm-zone"),hp:$("#vm-hp"),power:$("#vm-power"),cargo:$("#vm-cargo"),hpFill:$("#vm-hp-fill"),powerFill:$("#vm-power-fill"),cargoFill:$("#vm-cargo-fill"),tool:$("#vm-tool"),prompt:$("#vm-prompt"),event:$("#vm-event"),exit:$("#vm-exit-readout"),inventory:$("#vm-inventory")},lastHud={},CHUNK_TILES=8,CHUNK_SIZE=CHUNK_TILES*T,chunkCache=new Map(),particlePool=[],perf={show:false,fps:60,frame:0,update:0,render:0,chunk:0,visibleChunks:0,visibleTiles:0,dirtyChunks:0,lights:0,lastNow:performance.now()},quality=resolvedGraphicsProfile(),lightScale=quality==="high"?.5:quality==="medium"?.34:.25,lightCanvas=document.createElement("canvas"),lightCtx=lightCanvas.getContext("2d"),glowCache=new Map();
    lightCanvas.width=Math.ceil(W*lightScale);lightCanvas.height=Math.ceil(H*lightScale);
    let frameState=null,canvasRect=canvas.getBoundingClientRect(),hudClock=0,aiClock=0;
    const refreshCanvasRect=()=>{canvasRect=canvas.getBoundingClientRect()};
    const makeFrameState=(now,dt)=>{const camX=Math.max(0,Math.min(COLS*T-W,player.x-W/2)),camY=Math.max(0,Math.min(ROWS*T-H,player.y-H/2)),depth=depthAtY(player.y);return{now,dt,camX,camY,viewLeft:camX,viewRight:camX+W,viewTop:camY,viewBottom:camY+H,tileMinX:Math.max(0,Math.floor(camX/T)-1),tileMaxX:Math.min(COLS-1,Math.ceil((camX+W)/T)+1),tileMinY:Math.max(0,Math.floor(camY/T)-1),tileMaxY:Math.min(ROWS-1,Math.ceil((camY+H)/T)+1),depth,zone:zoneAt(depth),quality,pulseSlow:.82+.18*Math.sin(now/700),pulseFast:.82+.18*Math.sin(now/210)}};
    const chunkKey=(cx,cy)=>`${cx}:${cy}`,markChunkDirty=(tx,ty)=>{const chunk=chunkCache.get(chunkKey(Math.floor(tx/CHUNK_TILES),Math.floor(ty/CHUNK_TILES)));if(chunk&&!chunk.dirty){chunk.dirty=true;perf.dirtyChunks++}};
    const paintStaticTile=(g,c,tx,ty,lx,ly)=>{if(!c.solid)return;const m=materials[c.mat];g.fillStyle=m.color;g.fillRect(lx,ly,T,T);g.fillStyle="rgba(255,255,255,.055)";g.fillRect(lx+2,ly+2,T-4,3);g.strokeStyle="rgba(0,0,0,.28)";g.strokeRect(lx+.5,ly+.5,T-1,T-1);if((tx*17+ty*13)%7>1)return;const z=zoneAt(cellDepth(ty));g.save();g.translate(lx,ly);g.globalAlpha=.48;g.lineWidth=1.5;if(z.id==="upper"){g.strokeStyle="#b28755";g.beginPath();g.moveTo(4,24);g.lineTo(8,8);g.lineTo(25,7);g.stroke();g.fillStyle="#d5b06d";g.fillRect(6,5,4,4);g.fillRect(23,5,4,4)}else if(z.id==="deep"){g.strokeStyle="#8590a4";g.beginPath();g.moveTo(3,4);g.lineTo(13,12);g.lineTo(8,19);g.lineTo(26,27);g.moveTo(13,12);g.lineTo(22,7);g.stroke()}else if(z.id==="crystal"){g.strokeStyle="#31f5ff";g.beginPath();g.moveTo(3,27);g.lineTo(9,13);g.lineTo(13,24);g.lineTo(20,4);g.lineTo(27,27);g.stroke()}else if(z.id==="obsidian"){g.strokeStyle="#ff5b20";g.beginPath();g.moveTo(1,10);g.lineTo(11,14);g.lineTo(17,5);g.lineTo(28,9);g.moveTo(11,14);g.lineTo(8,28);g.stroke()}else{g.strokeStyle="#c35cff";g.beginPath();g.moveTo(4,4);g.lineTo(25,8);g.lineTo(19,25);g.lineTo(6,20);g.closePath();g.stroke()}g.restore()};
    const getChunk=(cx,cy)=>{const key=chunkKey(cx,cy);let chunk=chunkCache.get(key),solidCount=0;for(let ly=0;ly<CHUNK_TILES;ly++)for(let lx=0;lx<CHUNK_TILES;lx++){const tx=cx*CHUNK_TILES+lx,ty=cy*CHUNK_TILES+ly;if(tx<COLS&&ty<ROWS&&cells[ty][tx].solid)solidCount++}if(!chunk){const surface=document.createElement("canvas");surface.width=CHUNK_SIZE;surface.height=CHUNK_SIZE;chunk={canvas:surface,ctx:surface.getContext("2d"),cx,cy,dirty:true,solidCount:-1};chunkCache.set(key,chunk);perf.dirtyChunks++}else if(chunk.solidCount!==solidCount&&!chunk.dirty){chunk.dirty=true;perf.dirtyChunks++}if(chunk.dirty){const started=performance.now(),g=chunk.ctx;g.clearRect(0,0,CHUNK_SIZE,CHUNK_SIZE);for(let ly=0;ly<CHUNK_TILES;ly++)for(let lx=0;lx<CHUNK_TILES;lx++){const tx=cx*CHUNK_TILES+lx,ty=cy*CHUNK_TILES+ly;if(tx<COLS&&ty<ROWS)paintStaticTile(g,cells[ty][tx],tx,ty,lx*T,ly*T)}chunk.solidCount=solidCount;chunk.dirty=false;perf.dirtyChunks=Math.max(0,perf.dirtyChunks-1);perf.chunk=perf.chunk*.85+(performance.now()-started)*.15}return chunk};
    const drawTerrainChunks=fs=>{const minCX=Math.floor(fs.tileMinX/CHUNK_TILES),maxCX=Math.floor(fs.tileMaxX/CHUNK_TILES),minCY=Math.floor(fs.tileMinY/CHUNK_TILES),maxCY=Math.floor(fs.tileMaxY/CHUNK_TILES);perf.visibleChunks=0;for(let cy=minCY;cy<=maxCY;cy++)for(let cx=minCX;cx<=maxCX;cx++){const chunk=getChunk(cx,cy);ctx.drawImage(chunk.canvas,cx*CHUNK_SIZE,cy*CHUNK_SIZE);perf.visibleChunks++}perf.visibleTiles=(fs.tileMaxX-fs.tileMinX+1)*(fs.tileMaxY-fs.tileMinY+1)};
    const getGlow=(color,size=128)=>{const key=color+size;if(glowCache.has(key))return glowCache.get(key);const surface=document.createElement("canvas");surface.width=surface.height=size;const g=surface.getContext("2d"),r=size/2,gradient=g.createRadialGradient(r,r,0,r,r,r);gradient.addColorStop(0,color+"66");gradient.addColorStop(1,color+"00");g.fillStyle=gradient;g.fillRect(0,0,size,size);glowCache.set(key,surface);return surface};
    const getLampMask=()=>{const key="player-lamp";if(glowCache.has(key))return glowCache.get(key);const size=256,surface=document.createElement("canvas");surface.width=surface.height=size;const g=surface.getContext("2d"),r=size/2,beam=g.createRadialGradient(r,r,r*.18,r,r,r);beam.addColorStop(0,"rgba(0,0,0,1)");beam.addColorStop(.65,"rgba(0,0,0,.72)");beam.addColorStop(1,"rgba(0,0,0,0)");g.fillStyle=beam;g.fillRect(0,0,size,size);glowCache.set(key,surface);return surface};
    const solidAt=(x,y)=>{const cx=Math.floor(x/T),cy=Math.floor(y/T);return cx<0||cx>=COLS||cy<0||cy>=ROWS||cells[cy][cx]?.solid};
    const openMove=(x,y)=>!solidAt(x-player.r,y-player.r)&&!solidAt(x+player.r,y-player.r)&&!solidAt(x-player.r,y+player.r)&&!solidAt(x+player.r,y+player.r);
    const miningFx={dirt:{type:"dust",color:"#a78261",gravity:70},stone:{type:"chunk",color:"#78828e",gravity:150},hard:{type:"spark",color:"#d8e4ef",gravity:90},crystal:{type:"shard",color:"#31f5ff",gravity:55},obsidian:{type:"spark",color:"#ff7043",gravity:115},void:{type:"void",color:"#c35cff",gravity:-12}};
    const miningEffect=c=>c.res==="gold"?{type:"spark",color:"#ffe04b",gravity:80}:c.res==="uranium"?{type:"glow",color:"#72ff77",gravity:-18}:c.res==="voidstone"||c.res==="null"?{type:"void",color:"#c35cff",gravity:-12}:c.res==="crystal"||c.res==="prism"?{type:"shard",color:resourceDefs[c.res].color,gravity:45}:miningFx[c.mat]||miningFx.stone;
    const addParticles=(x,y,color,count=8,options={})=>{const cap=quality==="low"?45:quality==="medium"?85:135,type=options.type||"dust",gravity=options.gravity??90,power=options.power||130,priority=options.priority||0;if(particles.length>=cap&&priority<1)return;for(let i=0;i<count;i++){let p;if(particles.length>=cap){if(priority<1)break;p=particles.shift()}else p=particlePool.pop()||{};p.x=x;p.y=y;p.vx=(rand()-.5)*power;p.vy=(rand()-.62)*power;p.life=(options.life||.35)+rand()*(options.life||.35);p.color=color;p.type=type;p.gravity=gravity;p.size=options.size||2+rand()*3;p.rotation=rand()*Math.PI*2;p.spin=(rand()-.5)*12;p.priority=priority;particles.push(p)}};
    const miningTone=(c,final=false)=>{const fx=miningEffect(c),hardness=materials[c.mat].hard;if(fx.type==="shard")tone(final?980:760,final?.13:.035,"triangle",final?.025:.009);else if(fx.type==="glow")tone(final?620:420,final?.15:.04,"sine",final?.025:.009);else if(fx.type==="void")tone(final?145:105,final?.2:.05,"sawtooth",final?.026:.008);else if(fx.type==="spark")tone(final?520:260+hardness*45,final?.11:.025,"square",final?.022:.008);else tone(final?180:115+hardness*24,final?.1:.03,"triangle",final?.02:.007)};
    const floater=(text,x,y,color="#fff")=>floaters.push({text,x,y,life:1,color});
    const addCargo=(id,x,y)=>{if(cargoCount>=cargoMax){floater("CARGO FULL",x,y,"#ff4b6e");objects.push({type:"loose",res:id,x,y,used:false});return false}cargo[id]=(cargo[id]||0)+1;cargoCount++;discover("resources",id);floater(`+1 ${resourceDefs[id].name}`,x,y,resourceDefs[id].color);sfx(resourceDefs[id].rare?"win":"coin");updateInventory();return true};
    const updateInventory=()=>{const host=hud.inventory;if(!host)return;const entries=[];for(const id in cargo)if(cargo[id]>0)entries.push([id,cargo[id]]);host.innerHTML=entries.length?entries.map(([id,n])=>`<button data-vm-discard="${id}" style="--ore:${resourceDefs[id].color}"><i></i><b>${resourceDefs[id].name}</b><span>×${n}</span></button>`).join(""):'<small>CARGO EMPTY • CLICK AN ITEM TO DISCARD IT</small>';$$('[data-vm-discard]',host).forEach(button=>button.onclick=()=>{const id=button.dataset.vmDiscard;if(!cargo[id])return;cargo[id]--;cargoCount--;objects.push({type:"loose",res:id,x:player.x+Math.cos(player.angle)*28,y:player.y+Math.sin(player.angle)*28,used:false});updateInventory()});};
    const damage=(amount,label)=>{if(player.invuln>0||ended)return;const resist=1-vm.upgrades.suit*.09;player.hp-=amount*resist*(hard?1.2:1);player.invuln=.55;shake=8;floater(`-${Math.ceil(amount*resist)} ${label}`,player.x,player.y,"#ff4b6e");haptic([25,20,25]);if(player.hp<=0)finish(false,label)};
    const breakCell=(cx,cy,c)=>{const x=(cx+.5)*T,y=(cy+.5)*T,fx=miningEffect(c),hardness=materials[c.mat].hard;c.solid=false;markChunkDirty(cx,cy);blocks++;vm.stats.blocksMined++;addParticles(x,y,fx.color,18+hardness*5,{...fx,power:145+hardness*28,life:.48,size:3+hardness*.55,priority:1});breakBursts.push({x,y,color:fx.color,type:fx.type,life:.42,maxLife:.42});shake=Math.max(shake,3+hardness*2.4);drillRecoil=Math.max(drillRecoil,5+hardness*1.8);haptic(hardness>=3?[24,18,34]:[18,12,20]);miningTone(c,true);if(c.res)addCargo(c.res,x,y);if(c.hazard==="gas"){clouds.push({x,y,r:12,life:8});floater("GAS POCKET!",player.x,player.y,"#72ff77")}if(c.hazard==="electric")clouds.push({x,y,r:35,life:3,electric:true});mineCell=null;mineProgress=0;};
    const attemptMine=dt=>{const range=44+vm.upgrades.drill*2,ax=Math.cos(player.angle),ay=Math.sin(player.angle);let cx=0,cy=0,c=null,tx=player.x+ax*range,ty=player.y+ay*range;for(let distance=player.r+5;distance<=range;distance+=6){const px=player.x+ax*distance,py=player.y+ay*distance,nx=Math.floor(px/T),ny=Math.floor(py/T),candidate=cells[ny]?.[nx];if(candidate?.solid){cx=nx;cy=ny;c=candidate;tx=px;ty=py;break}}if(c){const tier=drillTier+(temporaryDrill>0?1:0),material=materials[c.mat];if(material.hard>tier){floater("DRILL TIER TOO LOW",tx,ty,"#ffb547");shake=Math.max(shake,2.5);drillRecoil=Math.max(drillRecoil,3);mineProgress=0;return}const key=`${cx}:${cy}`;if(mineCell!==key){mineCell=key;mineProgress=0;mineSoundClock=0}const speed=(32+vm.upgrades.drill*12)*(temporaryDrill>0?1.4:1)*(eventText.includes("POWER SURGE")?1.3:1),fx=miningEffect(c);mineProgress+=dt*speed;player.power-=dt*(2.4+material.hard*.75)*(1-vm.upgrades.drill*.055);drillRecoil=Math.max(drillRecoil,1.5+material.hard*.7+Math.sin(performance.now()*.09)*.8);shake=Math.max(shake,.35+material.hard*.24);if(rand()<dt*(20+material.hard*7))addParticles(tx,ty,fx.color,1,{...fx,power:75+material.hard*22,life:.22,size:2+material.hard*.4});if(mineSoundClock<=0){miningTone(c);mineSoundClock=Math.max(.07,.15-material.hard*.014)}if(mineProgress>=c.maxHp)breakCell(cx,cy,c);return}mineProgress=0;const enemy=enemies.find(e=>e.hp>0&&Math.hypot(e.x-tx,e.y-ty)<28);if(enemy){enemy.hp-=dt*(42+vm.upgrades.drill*9);enemy.hit=.1;player.power-=dt*1.8;if(enemy.hp<=0){discover("creatures",enemy.type);floater(`${enemy.type.toUpperCase()} CLEARED`,enemy.x,enemy.y,"#31f5ff");addParticles(enemy.x,enemy.y,"#ff3eb5",12,{type:"spark",power:170,life:.4})}}};
    const nearestObject=()=>{let best=null,bestD=Infinity;for(let i=0;i<objects.length;i++){const o=objects[i];if(o.used||o.type==="loose")continue;const dx=o.x-player.x,dy=o.y-player.y,d=dx*dx+dy*dy;if(d<bestD){best=o;bestD=d}}return best};
    const interact=()=>{const o=nearestObject();if(!o||Math.hypot(o.x-player.x,o.y-player.y)>48)return;if(o.type==="elevator")return finish(true,"EXTRACTION");if(o.type==="artifact"){o.used=true;if(runArtifacts.includes(o.id)){addCargo(maxDepth>700?"null":"prism",o.x,o.y);floater("DUPLICATE SIGNAL CONVERTED",o.x,o.y,"#ffb547")}else{runArtifacts.push(o.id);discover("artifacts",o.id);floater(`ARTIFACT • ${artifacts[o.id].name}`,o.x,o.y,"#ffe84c");sfx("win")}return}if(o.type==="log"){o.used=true;discover("logs",o.id);eventText=`${o.id.toUpperCase().replace("LOG", "MINER LOG ")} • ${["Something keeps knocking from the other side of the wall.","We stopped calling it rock three days ago.","Do not follow the purple lights.","The elevator returned before we called it."][Math.max(0,["log14","log27","log41","log63"].indexOf(o.id))]}`;eventLife=7;return}if(o.type==="battery"){o.used=true;player.power=Math.min(maxPower,player.power+28);floater("EMERGENCY POWER +28",o.x,o.y,"#72ff77");return}if(o.type==="crate"){o.used=true;const id=maxDepth>650?"voidstone":maxDepth>260?"gold":"copper";addCargo(id,o.x,o.y);if(rand()<.45)addCargo(id,o.x,o.y);return}discover("rooms",o.type);o.used=true;if(o.type==="camp"){player.power=Math.min(maxPower,player.power+35);player.hp=Math.min(maxHp,player.hp+25);floater("SUPPLIES RECOVERED",o.x,o.y,"#72ff77")}else if(o.type==="drill"){temporaryDrill=45;floater("DRILL OVERCLOCK • 45s",o.x,o.y,"#31f5ff")}else if(o.type==="shrine"){const cost=maxPower*.25;if(player.power>cost){player.power-=cost;const id=rand()<.22?"pearl":rand()<.5?"prism":"voidstone";addCargo(id,o.x,o.y);eventText="VOID SHRINE ACCEPTED";eventLife=4}else floater("THE SHRINE WANTS MORE POWER",o.x,o.y,"#c35cff")}else if(o.type==="vault"){Object.assign(player,{power:Math.min(maxPower,player.power+12)});eventText="CRYSTAL VAULT OPEN";eventLife=4}else if(o.type==="lab"){const available=artifactIds.filter(id=>!runArtifacts.includes(id));if(available.length){const id=available[Math.floor(rand()*available.length)];runArtifacts.push(id);discover("artifacts",id);eventText=`LAB RECOVERED • ${artifacts[id].name}`}else{addCargo("core",o.x,o.y);eventText="LAB DUPLICATE • ANCIENT CORE RECOVERED"}eventLife=5}else{player.y=Math.min((ROWS-2)*T,player.y+T*5);eventText="COLLAPSED SHAFT • SHORTCUT";eventLife=4}};
    const runEvent=()=>{const roll=rand();if(roll<.24){eventText="⚠ SEISMIC ACTIVITY • CAVE-IN RISK";shake=12;for(let i=0;i<6;i++)addParticles(player.x+(rand()-.5)*200,player.y-120,"#8b7769",8)}else if(roll<.48){eventText="ϟ POWER SURGE • MINING SPEED +30%";player.power=Math.min(maxPower,player.power+15)}else if(roll<.7){eventText="?? UNKNOWN SIGNAL DETECTED";const rare=objects.find(o=>!o.used&&(o.type==="artifact"||o.type==="shrine"));if(rare)floaters.push({text:"SIGNAL ↓",x:rare.x,y:rare.y,life:8,color:"#ff3eb5"})}else if(roll<.86){eventText="OXYGEN LEAK • POWER -12";player.power-=12}else{eventText="RARE MINERAL VEIN DETECTED";const cy=Math.min(ROWS-2,Math.floor(player.y/T)+5),cx=Math.max(1,Math.min(COLS-2,Math.floor(player.x/T)));cells[cy][cx].solid=true;cells[cy][cx].res=maxDepth>680?"null":"prism"}eventLife=5;eventClock=28+rand()*24;}
    function finish(success,cause="POWER FAILURE"){
      if(ended)return;ended=true;cancelAnimationFrame(raf);removeListeners();const value=Object.entries(cargo).reduce((n,[id,count])=>n+resourceDefs[id].value*count,0),secured=success?value:Math.floor(value*.15),artifactValue=success?runArtifacts.length*500:0,total=secured+artifactValue,oldDeepest=vm.stats.deepestDepth||0,isNewRecord=maxDepth>oldDeepest;
      vm.credits+=total;vm.stats.extractions+=success?1:0;vm.stats.failedRuns+=success?0:1;vm.stats.deepestDepth=Math.max(oldDeepest,maxDepth);vm.stats.totalValue+=total;vm.stats.artifactsFound+=success?runArtifacts.length:0;if(success&&cargoCount>=cargoMax)vm.stats.fullCargoExtractions++;if(success)runArtifacts.forEach(id=>{if(!vm.artifacts.includes(id))vm.artifacts.push(id)});saveData();
      setStage(`<section class="vm-report ${success?"success":"failed"}"><p class="eyebrow">${success?"================ EXTRACTION COMPLETE ================":"RUN SIGNAL LOST • "+cause}</p><div class="big-icon">${success?"🚀":"☠"}</div><h3>${success?"HAUL SECURED":"MOST CARGO LOST"}</h3><div class="vm-report-grid"><span>DEPTH REACHED<b>${maxDepth} m</b></span><span>BLOCKS MINED<b>${blocks}</b></span><span>CARGO VALUE<b>${value.toLocaleString()} CR</b></span><span>SECURED<b>${total.toLocaleString()} CR</b></span></div><div class="vm-haul">${Object.entries(cargo).filter(([,n])=>n).map(([id,n])=>`<span style="--ore:${resourceDefs[id].color}">${resourceDefs[id].name}<b>×${n}</b></span>`).join("")||"NO MATERIAL RECOVERED"}</div>${runArtifacts.length?`<p>ARTIFACTS: ${runArtifacts.map(id=>artifacts[id].name).join(" • ")}${success?"":" • LOST"}</p>`:""}<p>${isNewRecord?"NEW DEEPEST RECORD • ":""}${success?"THE COMPANY IS SURPRISED YOU RETURNED.":"INSURANCE RETAINED 15% OF MATERIAL VALUE."}</p><div class="game-actions"><button id="vm-again" class="pixel-btn primary">DESCEND AGAIN</button><button id="vm-upgrades" class="pixel-btn secondary">UPGRADES</button><button id="vm-home" class="pixel-btn secondary">TERMINAL</button></div></section>`);
      reward(Math.min(90,Math.floor(total/40)),Math.max(8,Math.floor(maxDepth/20)),{result:success?"win":"loss",score:maxDepth});$("#vm-again").onclick=startRun;$("#vm-upgrades").onclick=renderUpgrades;$("#vm-home").onclick=renderTitle;
    }
    const nearestExit=()=>{let best=null,bestD=Infinity;for(let i=0;i<objects.length;i++){const o=objects[i];if(o.type!=="elevator")continue;const dx=o.x-player.x,dy=o.y-player.y,d=dx*dx+dy*dy;if(d<bestD){best=o;bestD=d}}return best};
    const updateNavigation=()=>{const pcx=Math.floor(player.x/T),pcy=Math.floor(player.y/T);for(let y=Math.max(0,pcy-6);y<=Math.min(ROWS-1,pcy+6);y++)for(let x=Math.max(0,pcx-6);x<=Math.min(COLS-1,pcx+6);x++)if(Math.hypot(x-pcx,y-pcy)<=6.5)discovered[y][x]=1;const exit=nearestExit();if(exit){const angle=Math.atan2(exit.y-player.y,exit.x-player.x),arrows=["→","↘","↓","↙","←","↖","↑","↗"],arrow=arrows[(Math.round(angle/(Math.PI/4))+8)%8],distance=Math.round(Math.hypot(exit.x-player.x,exit.y-player.y)/T*10),readout=hud.exit;if(readout)readout.textContent=`${arrow} ${exit.kind==="emergency"?"EMERGENCY LIFT":"SURFACE EXIT"} • ${distance} m`}const now=performance.now();if(now-lastMapDraw<140)return;lastMapDraw=now;const mw=mapCanvas.width,mh=mapCanvas.height,sx=mw/COLS,sy=mh/ROWS;mapCtx.clearRect(0,0,mw,mh);mapCtx.fillStyle="#05030c";mapCtx.fillRect(0,0,mw,mh);for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++)if(discovered[y][x]){mapCtx.fillStyle=cells[y][x].solid?"#332640":"#17334a";mapCtx.fillRect(x*sx,y*sy,Math.max(1,sx+.25),Math.max(1,sy+.25))}(()=>{for(let i=0;i<objects.length;i++){const o=objects[i];if(o.type!=="elevator")continue;mapCtx.fillStyle=o.kind==="emergency"?"#ff3eb5":"#ffe84c";mapCtx.fillRect(o.x/T*sx-2,o.y/T*sy-2,5,5)}})();mapCtx.fillStyle="#31f5ff";mapCtx.beginPath();mapCtx.arc((player.x/T)*sx,(player.y/T)*sy,3,0,Math.PI*2);mapCtx.fill()};
    const setHud=(key,value,style=false)=>{if(lastHud[key]===value)return;lastHud[key]=value;const el=hud[key];if(!el)return;if(style)el.style.width=value;else el.textContent=value};
    const updateHud=(dt=1)=>{hudClock+=dt;if(hudClock<.075)return;hudClock=0;const depth=depthAtY(player.y),zone=zoneAt(depth),hp=Math.max(0,Math.ceil(player.hp)),power=Math.max(0,Math.ceil(player.power));maxDepth=Math.max(maxDepth,depth);discover("zones",zone.id);setHud("depth",`${depth} m`);setHud("zone",zone.name);setHud("hp",`${hp}/${maxHp}`);setHud("power",`${power}/${maxPower}`);setHud("cargo",`${cargoCount}/${cargoMax}`);setHud("hpFill",`${Math.max(0,player.hp/maxHp*100)}%`,true);setHud("powerFill",`${Math.max(0,player.power/maxPower*100)}%`,true);setHud("cargoFill",`${cargoCount/cargoMax*100}%`,true);setHud("tool",temporaryDrill>0?"OVERCLOCK +1":upgrades.drill.levels[vm.upgrades.drill]);updateNavigation()};
    const update=(dt)=>{if(gamePauseOpen)return;aiClock+=dt;const aiThink=aiClock>=.066;if(aiThink)aiClock=0;player.invuln=Math.max(0,player.invuln-dt);dashCooldown=Math.max(0,dashCooldown-dt);temporaryDrill=Math.max(0,temporaryDrill-dt);shake=Math.max(0,shake-dt*25);eventClock-=dt;eventLife-=dt;if(eventClock<=0)runEvent();const keyboardX=(keys.has("d")||keys.has("arrowright")?1:0)-(keys.has("a")||keys.has("arrowleft")?1:0),keyboardY=(keys.has("s")||keys.has("arrowdown")?1:0)-(keys.has("w")||keys.has("arrowup")?1:0),touchMoving=isMobileMode()&&Math.hypot(mobileInput.moveX,mobileInput.moveY)>.05;let dx=touchMoving?mobileInput.moveX:keyboardX,dy=touchMoving?mobileInput.moveY:keyboardY,len=Math.hypot(dx,dy),speed=125+vm.upgrades.mobility*10;if((keys.has("shift")||mobileInput.boost)&&dashCooldown<=0&&len){player.dash=.16;dashCooldown=Math.max(.65,1.25-vm.upgrades.mobility*.12);player.power-=3;haptic(10)}if(player.dash>0){player.dash-=dt;speed*=2.5}if(len){dx/=len;dy/=len;const nx=player.x+dx*speed*dt,ny=player.y+dy*speed*dt;if(openMove(nx,player.y))player.x=nx;if(openMove(player.x,ny))player.y=ny;player.power-=dt*.12;}if(isMobileMode()&&Math.hypot(mobileInput.aimX,mobileInput.aimY)>.18)player.angle=Math.atan2(mobileInput.aimY,mobileInput.aimX);else{const camX=player.x-W/2,camY=player.y-H/2;player.angle=Math.atan2(mouse.y+camY-player.y,mouse.x+camX-player.x)}if(mouse.down||keys.has(" ")||mobileInput.fire)attemptMine(dt);else{mineProgress=Math.max(0,mineProgress-dt*18);mineCell=null}if(keys.has("e")||mobileInput.interact){keys.delete("e");interact()}
      for(let i=0;i<objects.length;i++){const o=objects[i];if(o.used||o.type!=="loose")continue;const dx=o.x-player.x,dy=o.y-player.y;if(dx*dx+dy*dy<576&&addCargo(o.res,o.x,o.y))o.used=true}
      clouds.forEach(c=>{c.life-=dt;c.r=Math.min(c.electric?52:72,c.r+dt*11);if(Math.hypot(c.x-player.x,c.y-player.y)<c.r&&(c.electric?Math.sin(c.life*12)>.8:true))damage(c.electric?14:3,c.electric?"ARC":"GAS")});
      enemies.forEach(e=>{if(e.hp<=0)return;e.hit=Math.max(0,e.hit-dt);e.cooldown-=dt;e.phase+=dt;const d=Math.max(1,Math.hypot(player.x-e.x,player.y-e.y));if(e.type==="crawler"){if(aiThink&&d<125)e.awake=true;if(e.awake&&d<230){const speed=d<90?105:72,nx=e.x+(player.x-e.x)/d*speed*dt,ny=e.y+(player.y-e.y)/d*speed*dt;if(!solidAt(nx,ny)){e.x=nx;e.y=ny}}}else if(e.type==="mite"){if(aiThink&&d<185&&e.cooldown<=0){projectiles.push({x:e.x,y:e.y,vx:(player.x-e.x)/d*115,vy:(player.y-e.y)/d*115,life:2.2});e.cooldown=2.4;eventText="ELECTRIC CHARGE DETECTED";eventLife=Math.max(eventLife,1)}const homeD=Math.hypot(e.homeX-e.x,e.homeY-e.y);if(homeD>25){e.x+=(e.homeX-e.x)/homeD*20*dt;e.y+=(e.homeY-e.y)/homeD*20*dt}}else if(e.type==="wisp"){if(d<220){e.x+=(player.x-e.x)/d*45*dt+Math.cos(e.phase*3)*18*dt;e.y+=(player.y-e.y)/d*45*dt+Math.sin(e.phase*2)*18*dt;if(aiThink&&e.cooldown<=0&&d>80){e.x=player.x+(rand()-.5)*130;e.y=player.y+(rand()-.5)*130;e.cooldown=5+rand()*3;floater("PHASE SHIFT",e.x,e.y,"#c35cff")}}}else if(e.type==="stalker"&&d<260){e.awake=true;const speed=d<75?20:34,nx=e.x+(player.x-e.x)/d*speed*dt,ny=e.y+(player.y-e.y)/d*speed*dt;if(!solidAt(nx,ny)){e.x=nx;e.y=ny}}if(d<player.r+e.r+4)damage(e.type==="stalker"?19:e.type==="wisp"?13:8,e.type.toUpperCase())});
      projectiles.forEach(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.life-=dt;if(p.life>0&&Math.hypot(p.x-player.x,p.y-player.y)<player.r+5){p.life=0;damage(10,"CRYSTAL ARC")}});const under=cells[Math.floor(player.y/T)]?.[Math.floor(player.x/T)];if(under?.hazard==="lava")damage(9,"HEAT");if(player.power<=0){player.power=0;damage(5,"RESERVE FAILURE")}const ratio=player.power/maxPower,nextWarning=ratio<=0?3:ratio<=.1?2:ratio<=.2?1:0;if(nextWarning>powerWarning){powerWarning=nextWarning;eventText=nextWarning===1?"⚠ POWER LOW • 20%":nextWarning===2?"⚠ CRITICAL POWER • 10%":"⚠ RESERVE POWER ACTIVE • LIFE SUPPORT FAILING";eventLife=5;haptic([30,30,30])}if(ratio>.24)powerWarning=0;if(player.y>(ROWS-1)*T)finish(false,"LOST BELOW MAP");updateHud(dt);const o=nearestObject(),near=o&&Math.hypot(o.x-player.x,o.y-player.y)<48;extractionPrompt=near&&o.type==="elevator";const warning=ratio<=0?"⚠ RESERVE POWER • HP DRAIN ACTIVE":ratio<=.1?"⚠ CRITICAL POWER • RETURN NOW":ratio<=.2?"⚠ POWER LOW • FIND THE ELEVATOR":"",prompt=near?o.type==="elevator"?"E / USE • EXTRACT HAUL":o.type==="artifact"?"E / USE • RECOVER ARTIFACT":o.type==="log"?"E / USE • READ MINER LOG":`E / USE • ${o.type.toUpperCase()}`:warning,eventValue=eventLife>0?eventText:"";setHud("prompt",prompt);setHud("event",eventValue);if(lastHud.eventShow!==(eventLife>0)){lastHud.eventShow=eventLife>0;hud.event?.classList.toggle("show",eventLife>0)}};
    const drawDecoration=(d,now)=>{const x=d.x,y=d.y;ctx.save();ctx.translate(x,y);ctx.globalAlpha=.58;if(d.type==="beam"){ctx.strokeStyle="#8b6542";ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-12,15);ctx.lineTo(-12,-14);ctx.lineTo(12,-14);ctx.lineTo(12,15);ctx.stroke()}else if(d.type==="track"){ctx.strokeStyle="#6d7078";ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-15,-6);ctx.lineTo(15,-6);ctx.moveTo(-15,6);ctx.lineTo(15,6);ctx.stroke();for(let i=-12;i<14;i+=7)ctx.fillRect(i,-8,2,16)}else if(d.type==="lamp"){ctx.fillStyle="#ffe84c";ctx.shadowBlur=12;ctx.shadowColor="#ffe84c";ctx.fillRect(-3,-3,6,6)}else if(d.type==="crystalSpire"){ctx.fillStyle="#31f5ff";ctx.shadowBlur=9;ctx.shadowColor="#31f5ff";ctx.beginPath();ctx.moveTo(-8,12);ctx.lineTo(-3,-13);ctx.lineTo(2,8);ctx.lineTo(8,-7);ctx.lineTo(11,12);ctx.fill()}else if(d.type==="fracture"){ctx.strokeStyle="#ff7043";ctx.shadowBlur=7;ctx.shadowColor="#ff7043";ctx.beginPath();ctx.moveTo(-13,-9);ctx.lineTo(-3,-2);ctx.lineTo(-8,9);ctx.lineTo(8,14);ctx.stroke()}else if(d.type==="glyph"){ctx.strokeStyle="#c35cff";ctx.rotate(Math.sin(now/900+d.x)*.08);ctx.strokeRect(-8,-8,16,16);ctx.beginPath();ctx.moveTo(-8,8);ctx.lineTo(8,-8);ctx.stroke()}else{ctx.fillStyle=d.type==="ash"?"#8f7c72":d.type==="crystalDust"?"#31f5ff":d.type==="shadow"?"#7f38a8":"#6f6677";for(let i=0;i<4;i++)ctx.fillRect(Math.sin(i*9+d.x)*11,Math.cos(i*7+d.y)*9,2,2)}ctx.restore()};
    const drawCavern=(c,now)=>{const x=(c.cx+.5)*T,y=(c.cy+.5)*T,rx=c.rx*T,ry=c.ry*T;ctx.save();ctx.translate(x,y);ctx.globalAlpha=.42;if(c.type==="lake"){const g=ctx.createRadialGradient(0,0,8,0,0,rx);g.addColorStop(0,"#174c68");g.addColorStop(1,"#06172500");ctx.fillStyle=g;ctx.beginPath();ctx.ellipse(0,ry*.35,rx*.85,ry*.42,0,0,Math.PI*2);ctx.fill();ctx.strokeStyle="#31f5ff";for(let i=0;i<4;i++){ctx.beginPath();ctx.ellipse(Math.sin(now/700+i)*18,ry*.35+i*3,20+i*10,4,0,0,Math.PI*2);ctx.stroke()}}else if(c.type==="crystalCavern"){ctx.strokeStyle="#31f5ff";ctx.shadowBlur=20;ctx.shadowColor="#31f5ff";ctx.lineWidth=3;for(let i=-3;i<=3;i++){ctx.beginPath();ctx.moveTo(i*22,ry*.7);ctx.lineTo(i*18,-ry*.45-Math.abs(i%2)*25);ctx.stroke()}}else if(c.type==="lavaChamber"){ctx.fillStyle="#ff5420";ctx.shadowBlur=25;ctx.shadowColor="#ff5420";ctx.fillRect(-rx*.75,ry*.55,rx*1.5,8+Math.sin(now/180)*3)}else if(c.type==="industrial"){ctx.strokeStyle="#ffb547";ctx.lineWidth=5;ctx.strokeRect(-rx*.55,-ry*.55,rx*1.1,ry*1.1);ctx.fillStyle="#5d6470";for(let i=-2;i<=2;i++)ctx.fillRect(i*28-5,-ry*.6,10,ry*1.2)}else{ctx.strokeStyle="#c35cff";ctx.shadowBlur=18;ctx.shadowColor="#c35cff";ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-rx*.6,ry*.65);ctx.lineTo(-rx*.35,-ry*.6);ctx.lineTo(0,-ry*.82);ctx.lineTo(rx*.35,-ry*.6);ctx.lineTo(rx*.6,ry*.65);ctx.stroke();for(let i=0;i<5;i++){ctx.rotate(Math.PI*2/5);ctx.strokeRect(rx*.2,-5,18,10)}}ctx.restore()};
    const drawMiningDamage=now=>{if(!mineCell)return;const [cx,cy]=mineCell.split(":").map(Number),c=cells[cy]?.[cx];if(!c?.solid)return;const ratio=Math.min(1,mineProgress/c.maxHp),stages=Math.max(1,Math.ceil(ratio*6)),px=cx*T,py=cy*T,fx=miningEffect(c);ctx.save();ctx.translate(px+T/2,py+T/2);ctx.strokeStyle=fx.color;ctx.shadowColor=fx.color;ctx.shadowBlur=4+ratio*11;ctx.lineWidth=1+ratio*1.5;for(let i=0;i<stages;i++){const angle=i*2.399+cx*.71+cy*.37,length=5+ratio*12+(i%3)*2;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(angle)*length*.48,Math.sin(angle)*length*.48);ctx.lineTo(Math.cos(angle+.28)*length,Math.sin(angle+.28)*length);ctx.stroke()}ctx.globalAlpha=.35+.45*ratio;ctx.beginPath();ctx.arc(0,0,11+ratio*4,-Math.PI/2,-Math.PI/2+Math.PI*2*ratio);ctx.stroke();if(fx.type==="void"){ctx.globalCompositeOperation="difference";ctx.fillStyle="#fff";for(let i=0;i<stages;i++)ctx.fillRect(-13+((i*11+Math.floor(now/50))%25),-12+i*4,5,2)}ctx.restore()};
    const drawWorldBase=fs=>{const {zone,camX,camY,tileMinX:x0,tileMaxX:x1,tileMinY:y0,tileMaxY:y1,now}=fs,sx=shake?(rand()-.5)*shake:0,sy=shake?(rand()-.5)*shake:0;ctx.save();ctx.translate(sx,sy);ctx.fillStyle=zone.bg;ctx.fillRect(-10,-10,W+20,H+20);ctx.translate(-camX,-camY);drawTerrainChunks(fs);
      for(let y=y0;y<=y1;y++)for(let x=x0;x<=x1;x++){const c=cells[y][x],px=x*T,py=y*T;if(c.solid){if(c.hazard==="gas"){ctx.fillStyle="#72ff7744";ctx.fillRect(px+5,py+5,7,7)}if(c.hazard==="electric"){ctx.strokeStyle="#31f5ff";ctx.beginPath();ctx.moveTo(px+5,py+7);ctx.lineTo(px+16,py+15);ctx.lineTo(px+9,py+25);ctx.stroke()}if(mineCell===`${x}:${y}`){ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.strokeRect(px+3,py+3,T-6,T-6);ctx.fillStyle="#fff";ctx.fillRect(px+3,py+T-5,(T-6)*Math.min(1,mineProgress/c.maxHp),2)}}else if(c.hazard==="lava"){ctx.fillStyle="#ff5b20";ctx.fillRect(px+2,py+2,T-4,T-4)}}
      drawMiningDamage(now);for(let i=0;i<caverns.length;i++){const c=caverns[i];if(Math.abs(c.cx*T-camX-W/2)<W&&Math.abs(c.cy*T-camY-H/2)<H)drawCavern(c,now)}for(let i=0;i<decorations.length;i++){const d=decorations[i];if(d.x>camX-40&&d.x<camX+W+40&&d.y>camY-40&&d.y<camY+H+40)drawDecoration(d,now)}for(let y=y0;y<=y1;y++)for(let x=x0;x<=x1;x++){const c=cells[y][x],px=x*T,py=y*T;if(!c.solid&&c.hazard==="lava"){ctx.strokeStyle="#ffd15c";ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(px+2,py+10+Math.sin(now/170+x)*4);ctx.bezierCurveTo(px+9,py+4,px+18,py+17,px+28,py+8+Math.cos(now/210+y)*4);ctx.stroke()}}if(quality!=="low")for(let i=0;i<ambientMotes.length;i++){const m=ambientMotes[i];if(m.x<=camX||m.x>=camX+W||m.y<=camY||m.y>=camY+H)continue;const drift=Math.sin(now/900+m.phase)*7;ctx.globalAlpha=.18+.18*Math.sin(now/700+m.phase);ctx.fillStyle=m.zone==="crystal"?"#31f5ff":m.zone==="obsidian"?"#ff7043":m.zone==="void"?"#c35cff":"#c9b69d";ctx.fillRect(m.x+drift,m.y-Math.cos(now/1100+m.phase)*8,m.size,m.size)}ctx.globalAlpha=1;
      for(let i=0;i<objects.length;i++){const o=objects[i];if(o.used||o.scene||o.x<=camX-70||o.x>=camX+W+70||o.y<=camY-70||o.y>=camY+H+70)continue;const colors={elevator:"#ffe84c",artifact:"#fff",log:"#31f5ff",battery:"#72ff77",crate:"#ffb547",loose:resourceDefs[o.res]?.color};ctx.fillStyle=colors[o.type]||"#fff";if(o.type==="crate"){ctx.fillRect(o.x-10,o.y-8,20,16);ctx.strokeStyle="#3f2b20";ctx.strokeRect(o.x-10,o.y-8,20,16)}else if(o.type==="log"){ctx.fillRect(o.x-8,o.y-6,16,12);ctx.fillStyle="#07121b";ctx.fillRect(o.x-5,o.y-3,10,6)}else if(o.type==="battery"){ctx.fillRect(o.x-6,o.y-11,12,22);ctx.fillStyle="#07120c";ctx.fillRect(o.x-2,o.y-7,4,14)}else{ctx.beginPath();ctx.arc(o.x,o.y,o.type==="elevator"?18:9,0,Math.PI*2);ctx.fill()}ctx.fillStyle="#090516";ctx.font="bold 12px monospace";ctx.textAlign="center";ctx.fillText(o.type==="elevator"?"E":o.type==="artifact"?artifacts[o.id].icon:o.type==="loose"?"◆":"",o.x,o.y+4)}
      for(let i=0;i<clouds.length;i++){const c=clouds[i];if(c.life<=0||c.x<camX-80||c.x>camX+W+80||c.y<camY-80||c.y>camY+H+80)continue;const charged=c.electric&&Math.sin(c.life*12)>.55;ctx.globalAlpha=Math.min(charged?.75:.48,c.life/2);ctx.strokeStyle=c.electric?"#31f5ff":"#72ff77";ctx.fillStyle=c.electric?"#31f5ff44":"#72ff7738";ctx.lineWidth=2;ctx.beginPath();ctx.arc(c.x,c.y,c.r,0,Math.PI*2);ctx.fill();ctx.stroke()}ctx.globalAlpha=1;for(let i=0;i<projectiles.length;i++){const p=projectiles[i];if(p.life<=0||p.x<camX-20||p.x>camX+W+20||p.y<camY-20||p.y>camY+H+20)continue;ctx.fillStyle="#31f5ff";ctx.beginPath();ctx.arc(p.x,p.y,4,0,Math.PI*2);ctx.fill()}
      for(let i=0;i<breakBursts.length;i++){const b=breakBursts[i],t=1-b.life/b.maxLife,r=8+t*34;ctx.globalAlpha=Math.max(0,b.life/b.maxLife);ctx.strokeStyle=b.color;ctx.lineWidth=b.type==="void"?4:2;ctx.beginPath();ctx.arc(b.x,b.y,r,0,Math.PI*2);ctx.stroke();if(b.type==="void"){ctx.globalCompositeOperation="difference";ctx.strokeRect(b.x-r*.6,b.y-r*.35,r*1.2,r*.7);ctx.globalCompositeOperation="source-over"}}for(let i=0;i<particles.length;i++){const p=particles[i];ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.rotation||0);ctx.globalAlpha=Math.min(1,p.life*3);ctx.fillStyle=p.color;ctx.strokeStyle=p.color;if(p.type==="spark"){ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-p.size*1.8,0);ctx.lineTo(p.size*1.8,0);ctx.stroke()}else if(p.type==="shard"){ctx.beginPath();ctx.moveTo(0,-p.size*1.8);ctx.lineTo(p.size,p.size);ctx.lineTo(-p.size*.7,p.size*.4);ctx.closePath();ctx.fill()}else if(p.type==="void"){ctx.fillRect(-p.size*1.5,-1,p.size*3,2);ctx.globalAlpha*=.45;ctx.fillRect(-1,-p.size*1.5,2,p.size*3)}else if(p.type==="chunk")ctx.fillRect(-p.size/2,-p.size/2,p.size,p.size);else{ctx.globalAlpha*=.72;ctx.beginPath();ctx.arc(0,0,p.size,0,Math.PI*2);ctx.fill()}ctx.restore()}ctx.globalAlpha=1;for(let i=0;i<floaters.length;i++){const f=floaters[i];ctx.globalAlpha=Math.min(1,f.life*2);ctx.fillStyle=f.color;ctx.font="bold 12px monospace";ctx.textAlign="center";ctx.fillText(f.text,f.x,f.y)}ctx.globalAlpha=1;ctx.restore();
    };
    const landmarkPalette={camp:["#ffb547","#79593b"],vault:["#31f5ff","#174d60"],lab:["#72ff77","#28364a"],shaft:["#ff7043","#6a5142"],shrine:["#c35cff","#271035"],drill:["#ffe84c","#525d6b"]};
    const drawLandmarkUpgrade=(o,now)=>{const [accent,base]=landmarkPalette[o.type]||["#fff","#333"],pulse=.75+.25*Math.sin(now/330+o.x*.01);ctx.save();ctx.translate(o.x,o.y);ctx.strokeStyle=accent;ctx.fillStyle=base;ctx.shadowColor=accent;ctx.shadowBlur=12*pulse;ctx.lineWidth=2;ctx.globalAlpha=.92;if(o.type==="camp"){ctx.fillStyle="#231810";ctx.fillRect(-48,16,96,8);ctx.strokeStyle="#8d6744";ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-42,21);ctx.lineTo(-34,-29);ctx.lineTo(22,-29);ctx.lineTo(36,21);ctx.stroke();ctx.fillStyle="#5b3926";ctx.beginPath();ctx.moveTo(-31,15);ctx.lineTo(-18,-18);ctx.lineTo(-3,15);ctx.fill();ctx.fillStyle="#ffb547";ctx.shadowBlur=18;ctx.fillRect(6,3,6,13);ctx.fillStyle="#8893a0";ctx.fillRect(20,5,22,13);ctx.strokeStyle="#343a43";ctx.strokeRect(20,5,22,13);ctx.fillStyle="#d6e0ea";ctx.fillRect(-43,-24,15,5)}else if(o.type==="vault"){ctx.fillStyle="#07151e";ctx.fillRect(-48,-26,96,48);ctx.strokeRect(-48,-26,96,48);ctx.lineWidth=4;ctx.strokeRect(-31,-19,62,38);ctx.beginPath();ctx.arc(0,0,12,0,Math.PI*2);ctx.stroke();ctx.fillStyle=accent;for(let i=-3;i<=3;i++){const h=16+Math.abs(i%3)*9;ctx.beginPath();ctx.moveTo(i*13-5,22);ctx.lineTo(i*13,-h);ctx.lineTo(i*13+6,22);ctx.fill()}}else if(o.type==="lab"){ctx.fillStyle="#111d28";ctx.fillRect(-52,-27,104,52);ctx.strokeRect(-52,-27,104,52);ctx.fillStyle="#061018";ctx.fillRect(-43,-18,30,22);ctx.strokeStyle="#31f5ff";ctx.strokeRect(-43,-18,30,22);ctx.beginPath();ctx.moveTo(-39,-3);ctx.lineTo(-32,-10);ctx.lineTo(-25,-6);ctx.lineTo(-18,-15);ctx.stroke();ctx.strokeStyle="#ff3eb5";ctx.beginPath();ctx.ellipse(18,2,15,22,0,0,Math.PI*2);ctx.stroke();ctx.fillStyle="#c35cff66";ctx.fill();ctx.fillStyle=accent;ctx.fillRect(-43,12,17,5);ctx.fillRect(-20,12,10,5);ctx.fillStyle="#eefaff";ctx.fillRect(37,-20,7,7)}else if(o.type==="shaft"){ctx.fillStyle="#050308";ctx.fillRect(-49,-30,98,57);ctx.strokeStyle="#855f3e";ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(-43,27);ctx.lineTo(-34,-27);ctx.lineTo(35,-27);ctx.lineTo(44,27);ctx.moveTo(-30,-14);ctx.lineTo(33,-11);ctx.stroke();ctx.strokeStyle="#7f8791";ctx.lineWidth=2;for(let x=-26;x<=28;x+=12){ctx.beginPath();ctx.moveTo(x,-25);ctx.lineTo(x+4,25);ctx.stroke()}ctx.fillStyle="#ff7043";for(let i=0;i<5;i++)ctx.fillRect(-31+i*15,15-(i%2)*7,10,7)}else if(o.type==="shrine"){ctx.globalAlpha=.84;ctx.fillStyle="#08030c";ctx.beginPath();ctx.moveTo(-47,23);ctx.lineTo(-29,-17);ctx.lineTo(-13,-6);ctx.lineTo(0,-35);ctx.lineTo(15,-6);ctx.lineTo(31,-17);ctx.lineTo(48,23);ctx.closePath();ctx.fill();ctx.stroke();ctx.globalAlpha=.68;for(let i=0;i<4;i++){ctx.save();ctx.rotate(now/1100+i*Math.PI/2);ctx.strokeRect(25,-5,12,10);ctx.restore()}ctx.globalAlpha=1;ctx.fillStyle="#ff3eb5";ctx.beginPath();ctx.arc(0,-5,5*pulse,0,Math.PI*2);ctx.fill()}else{ctx.fillStyle="#333d49";ctx.fillRect(-48,3,74,23);ctx.strokeRect(-48,3,74,23);ctx.fillStyle="#151b23";ctx.beginPath();ctx.arc(-30,26,11,0,Math.PI*2);ctx.arc(11,26,11,0,Math.PI*2);ctx.fill();ctx.fillStyle=accent;ctx.beginPath();ctx.moveTo(26,14);ctx.lineTo(54,-5);ctx.lineTo(54,32);ctx.closePath();ctx.fill();ctx.strokeStyle="#ff3eb5";ctx.beginPath();ctx.moveTo(-38,0);ctx.bezierCurveTo(-25,-25,3,24,27,-10);ctx.stroke()}ctx.globalAlpha=.34;ctx.fillStyle=accent;ctx.beginPath();ctx.ellipse(0,25,58,15,0,0,Math.PI*2);ctx.fill();ctx.restore()};
    const drawOreUpgrade=(c,x,y,now)=>{const id=c.res;if(!id)return;const color=resourceDefs[id].color,pulse=.82+.18*Math.sin(now/210+x*.07+y*.05);ctx.save();ctx.translate(x+T/2,y+T/2);ctx.strokeStyle=color;ctx.fillStyle=color;ctx.shadowColor=color;ctx.shadowBlur=(resourceDefs[id].rare?20:11)*pulse;ctx.lineWidth=2;if(["iron","copper","silver"].includes(id)){ctx.lineWidth=id==="iron"?4:3;ctx.beginPath();ctx.moveTo(-13,8);ctx.bezierCurveTo(-8,-7,-3,5,2,-9);ctx.bezierCurveTo(7,-18,9,3,14,-11);ctx.stroke();ctx.globalAlpha=.65;ctx.beginPath();ctx.moveTo(-10,12);ctx.lineTo(-1,4);ctx.lineTo(9,11);ctx.stroke()}else if(id==="gold"){[[ -9,-8,5],[2,-11,4],[-3,1,7],[8,5,5],[-10,9,4]].forEach(([px,py,s])=>{ctx.rotate(.08);ctx.fillRect(px,py,s,s);ctx.strokeStyle="#fff3a0";ctx.strokeRect(px,py,s,s)})}else if(["crystal","prism"].includes(id)){[[-10,11,-6,-12],[-1,12,3,-17],[8,12,12,-7]].forEach(([bx,by,tx,ty])=>{ctx.beginPath();ctx.moveTo(bx-4,by);ctx.lineTo(tx,ty);ctx.lineTo(bx+5,by);ctx.closePath();ctx.fill();ctx.strokeStyle="#d7fbff";ctx.stroke()})}else if(id==="uranium"){ctx.globalAlpha=.28;ctx.beginPath();ctx.arc(0,0,15*pulse,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,7,0,Math.PI*2);ctx.stroke();for(let i=0;i<3;i++){ctx.save();ctx.rotate(i*Math.PI*2/3+now/900);ctx.fillRect(-2,-14,4,8);ctx.restore()}}else if(id==="voidstone"){ctx.fillStyle="#020104";ctx.fillRect(-13,-13,26,26);ctx.strokeRect(-13,-13,26,26);ctx.beginPath();ctx.moveTo(-13,-4);ctx.lineTo(-3,1);ctx.lineTo(-8,13);ctx.moveTo(-3,1);ctx.lineTo(11,-10);ctx.moveTo(1,5);ctx.lineTo(13,9);ctx.stroke()}else if(id==="null"){ctx.globalCompositeOperation="difference";ctx.fillStyle="#fff";for(let i=0;i<5;i++){ctx.save();ctx.rotate(i*Math.PI/5+now/1300);ctx.fillRect(2,-2,13-i,4);ctx.restore()}ctx.globalCompositeOperation="source-over";ctx.strokeRect(-12,-12,24,24);ctx.rotate(now/800);ctx.strokeRect(-8,-8,16,16)}else if(id==="pearl"){ctx.globalAlpha=.3;ctx.beginPath();ctx.arc(0,0,15*pulse,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,0,7,0,Math.PI*2);ctx.fill();ctx.strokeStyle=color;ctx.beginPath();ctx.ellipse(0,0,15,6,now/800,0,Math.PI*2);ctx.stroke()}else{ctx.rotate(Math.PI/4);ctx.fillRect(-9,-9,18,18);ctx.strokeStyle="#fff";ctx.strokeRect(-6,-6,12,12)}ctx.restore()};
    const drawCreatureUpgrade=(e,now)=>{ctx.save();ctx.translate(e.x,e.y);const hit=e.hit>0,color=e.type==="mite"?"#31f5ff":e.type==="wisp"?"#c35cff":e.type==="stalker"?"#a91fff":"#ff7043";ctx.strokeStyle=hit?"#fff":color;ctx.fillStyle=hit?"#fff":color;ctx.shadowColor=color;ctx.shadowBlur=e.type==="stalker"?24:14;ctx.lineWidth=2;if(e.type==="crawler"){ctx.fillStyle="#2b1010";ctx.beginPath();ctx.ellipse(0,2,15,7,0,0,Math.PI*2);ctx.fill();ctx.stroke();for(let i=-2;i<=2;i++){const kick=Math.sin(now/70+i*2)*5;ctx.beginPath();ctx.moveTo(i*5,3);ctx.lineTo(i*8+kick,14);ctx.moveTo(i*5,0);ctx.lineTo(i*8-kick,-9);ctx.stroke()}ctx.fillStyle="#ffe84c";ctx.fillRect(7,-2,3,2);ctx.fillRect(12,-1,3,2)}else if(e.type==="mite"){ctx.rotate(now/650);ctx.fillStyle="#0a2631";ctx.beginPath();for(let i=0;i<12;i++){const r=i%2?7:17;ctx.lineTo(Math.cos(i*Math.PI/6)*r,Math.sin(i*Math.PI/6)*r)}ctx.closePath();ctx.fill();ctx.stroke();ctx.rotate(-now/300);ctx.fillStyle="#fff";ctx.fillRect(-3,-3,6,6);if(Math.sin(now/80)>.5){ctx.strokeStyle="#fff";ctx.beginPath();ctx.moveTo(-18,0);ctx.lineTo(-23,-5);ctx.moveTo(18,0);ctx.lineTo(23,5);ctx.stroke()}}else if(e.type==="wisp"){ctx.globalAlpha=.55+.3*Math.sin(now/120+e.phase);for(let i=3;i>0;i--){ctx.globalAlpha*=.76;ctx.beginPath();ctx.arc(Math.sin(now/210+i)*i*3,i*7,5+i*3,0,Math.PI*2);ctx.stroke()}ctx.globalAlpha=.9;ctx.beginPath();ctx.arc(0,0,10,0,Math.PI*2);ctx.fill();ctx.fillStyle="#fff";ctx.fillRect(-4,-2,2,2);ctx.fillRect(2,-2,2,2)}else{ctx.globalAlpha=e.awake?.95:.48;ctx.fillStyle="#030104";ctx.beginPath();ctx.moveTo(0,-30);ctx.quadraticCurveTo(17,-16,11,4);ctx.lineTo(8,29);ctx.lineTo(2,18);ctx.lineTo(-4,30);ctx.lineTo(-11,5);ctx.quadraticCurveTo(-18,-16,0,-30);ctx.fill();ctx.stroke();ctx.fillStyle="#ff3eb5";ctx.fillRect(-7,-16,5,2);ctx.fillRect(3,-16,5,2);ctx.globalAlpha=.22;ctx.beginPath();ctx.arc(0,0,28+Math.sin(now/200)*5,0,Math.PI*2);ctx.stroke()}ctx.restore()};
    const drawMinerUpgrade=now=>{const tier=drillTier,accent=tier>=4?"#c35cff":tier>=3?"#ff7043":tier>=2?"#ffe84c":"#31f5ff";ctx.save();ctx.translate(player.x,player.y);ctx.rotate(player.angle);ctx.translate(-drillRecoil*.3,0);ctx.fillStyle="#172133";ctx.strokeStyle=accent;ctx.shadowColor=accent;ctx.shadowBlur=10;ctx.lineWidth=2;ctx.fillRect(-13,-9,19,18);ctx.strokeRect(-13,-9,19,18);ctx.fillStyle="#273d55";ctx.fillRect(-17,-7,6,14);ctx.fillStyle="#d8f7ff";ctx.beginPath();ctx.arc(-2,0,7,-Math.PI/2,Math.PI/2);ctx.fill();ctx.stroke();ctx.fillStyle=accent;ctx.fillRect(5,-4,13+tier*3,8);ctx.fillStyle="#050713";for(let i=0;i<tier;i++)ctx.fillRect(10+i*4,-2,2,4);ctx.fillStyle="#fff";ctx.beginPath();ctx.moveTo(18+tier*3,0);ctx.lineTo(28+tier*4,-7);ctx.lineTo(26+tier*4,0);ctx.lineTo(28+tier*4,7);ctx.closePath();ctx.fill();ctx.strokeStyle=accent;ctx.globalAlpha=.38;ctx.beginPath();ctx.moveTo(2,0);ctx.lineTo(76,-28);ctx.lineTo(76,28);ctx.closePath();ctx.fillStyle=accent;ctx.fill();ctx.restore()};
    const drawOreAndAtmosphere=fs=>{const {now,zone,camX,camY,tileMinX:x0,tileMaxX:x1,tileMinY:y0,tileMaxY:y1}=fs;ctx.save();ctx.translate(-camX,-camY);ctx.globalCompositeOperation="screen";for(let y=y0;y<=y1;y++)for(let x=x0;x<=x1;x++){const c=cells[y][x];if(c.solid&&c.res)drawOreUpgrade(c,x*T,y*T,now)}ctx.restore();ctx.save();ctx.globalCompositeOperation="screen";const tint={upper:"#ffb547",deep:"#6c82a8",crystal:"#31f5ff",obsidian:"#ff5420",void:"#8629c9"}[zone.id];ctx.globalAlpha=zone.id==="void"?.09:.045;ctx.fillStyle=tint;ctx.fillRect(0,0,W,H);if(quality!=="low"){ctx.globalAlpha=.2;const count=quality==="high"?(zone.id==="void"?22:zone.id==="crystal"?15:9):6;for(let i=0;i<count;i++){const px=(i*83+now*(zone.id==="void"?.008:.015))%W,py=(i*137+Math.sin(now/700+i)*35+H)%H,size=zone.id==="void"?2+(i%5):1+(i%3);ctx.fillRect(px,py,size,zone.id==="void"?1:size)}}ctx.restore()};
    const drawLighting=fs=>{const lw=lightCanvas.width,lh=lightCanvas.height,s=lightScale,g=lightCtx;g.setTransform(1,0,0,1,0,0);g.clearRect(0,0,lw,lh);g.fillStyle=quality==="low"?"rgba(2,1,8,.68)":"rgba(2,1,8,.74)";g.fillRect(0,0,lw,lh);g.globalCompositeOperation="destination-out";const px=(player.x-fs.camX)*s,py=(player.y-fs.camY)*s,rad=(145+vm.upgrades.light*24)*s;g.drawImage(getLampMask(),px-rad,py-rad,rad*2,rad*2);g.globalCompositeOperation="screen";let lightCount=0,maxLights=quality==="high"?18:quality==="medium"?10:6;for(let y=fs.tileMinY;y<=fs.tileMaxY&&lightCount<maxLights;y++)for(let x=fs.tileMinX;x<=fs.tileMaxX&&lightCount<maxLights;x++){const c=cells[y][x];if(!c.res||!(c.res==="crystal"||c.res==="uranium"||c.res==="voidstone"||c.res==="prism"||c.res==="null"||c.res==="pearl"))continue;const color=resourceDefs[c.res].color,sprite=getGlow(color),size=(resourceDefs[c.res].rare?92:64)*s,lx=((x+.5)*T-fs.camX)*s,ly=((y+.5)*T-fs.camY)*s;g.drawImage(sprite,lx-size/2,ly-size/2,size,size);lightCount++}for(let i=0;i<objects.length&&lightCount<maxLights;i++){const o=objects[i];if(!o.scene||o.x<fs.viewLeft-100||o.x>fs.viewRight+100||o.y<fs.viewTop-100||o.y>fs.viewBottom+100)continue;const color=(landmarkPalette[o.type]||["#fff"])[0],sprite=getGlow(color),size=(o.type==="shrine"?150:110)*s,lx=(o.x-fs.camX)*s,ly=(o.y-fs.camY)*s;g.drawImage(sprite,lx-size/2,ly-size/2,size,size);lightCount++}g.globalCompositeOperation="source-over";perf.lights=lightCount;ctx.save();ctx.globalAlpha=.92;ctx.imageSmoothingEnabled=true;ctx.drawImage(lightCanvas,0,0,W,H);ctx.restore()};
    const drawPerf=fs=>{if(!perf.show)return;ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.globalAlpha=.88;ctx.fillStyle="#05030c";ctx.fillRect(12,12,238,174);ctx.globalAlpha=1;ctx.strokeStyle="#31f5ff";ctx.strokeRect(12.5,12.5,237,173);ctx.fillStyle="#31f5ff";ctx.font="bold 12px monospace";ctx.textAlign="left";const lines=["VOID MINER PERF",`FPS: ${perf.fps.toFixed(0)}   FRAME: ${perf.frame.toFixed(1)} ms`,`UPDATE: ${perf.update.toFixed(1)} ms`,`RENDER: ${perf.render.toFixed(1)} ms`,`CHUNKS: ${perf.visibleChunks}   TILES: ${perf.visibleTiles}`,`DIRTY: ${perf.dirtyChunks}   REDRAW: ${perf.chunk.toFixed(1)} ms`,`ENEMIES: ${enemies.length}   SHOTS: ${projectiles.length}`,`PARTICLES: ${particles.length}   LIGHTS: ${perf.lights}`,`QUALITY: ${quality.toUpperCase()}`];for(let i=0;i<lines.length;i++)ctx.fillText(lines[i],24,34+i*17);ctx.restore()};
    const drawActors=fs=>{ctx.save();ctx.globalCompositeOperation="source-over";ctx.translate(-fs.camX,-fs.camY);for(let i=0;i<objects.length;i++){const o=objects[i];if(!o.scene||o.used||o.x<fs.viewLeft-90||o.x>fs.viewRight+90||o.y<fs.viewTop-90||o.y>fs.viewBottom+90)continue;drawLandmarkUpgrade(o,fs.now)}for(let i=0;i<enemies.length;i++){const e=enemies[i];if(e.hp<=0||e.x<fs.viewLeft-50||e.x>fs.viewRight+50||e.y<fs.viewTop-50||e.y>fs.viewBottom+50)continue;drawCreatureUpgrade(e,fs.now)}drawMinerUpgrade(fs.now);ctx.restore()};
    // Stable render order: cached world -> upgraded ores/atmosphere -> landmarks/enemies/player -> lighting.
    const renderFrame=fs=>{drawWorldBase(fs);drawOreAndAtmosphere(fs);drawActors(fs);drawLighting(fs)};
    const frame=now=>{if(ended)return;if(document.hidden||gamePauseOpen){last=now;raf=requestAnimationFrame(frame);return}const minFrame=window.GubuntuPerf?.targetFrameInterval?.()||0;if(now-last<minFrame){raf=requestAnimationFrame(frame);return}const frameStarted=performance.now(),dt=Math.min(.04,Math.max(0,(now-last)/1000));last=now;frameState=makeFrameState(now,dt);mineSoundClock=Math.max(0,mineSoundClock-dt);drillRecoil=Math.max(0,drillRecoil-dt*28);const updateStarted=performance.now();update(dt);for(let i=0;i<particles.length;i++){const p=particles[i];p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=Math.pow(.9,dt*8);p.vy+=(p.gravity??90)*dt;p.rotation=(p.rotation||0)+(p.spin||0)*dt;p.life-=dt}for(let i=0;i<breakBursts.length;i++)breakBursts[i].life-=dt;for(let i=0;i<floaters.length;i++){floaters[i].y-=22*dt;floaters[i].life-=dt}for(let i=particles.length-1;i>=0;i--)if(particles[i].life<=0)particlePool.push(particles.splice(i,1)[0]);for(let i=breakBursts.length-1;i>=0;i--)if(breakBursts[i].life<=0)breakBursts.splice(i,1);for(let i=floaters.length-1;i>=0;i--)if(floaters[i].life<=0)floaters.splice(i,1);for(let i=clouds.length-1;i>=0;i--)if(clouds[i].life<=0)clouds.splice(i,1);for(let i=projectiles.length-1;i>=0;i--)if(projectiles[i].life<=0)projectiles.splice(i,1);const updateMs=performance.now()-updateStarted,renderStarted=performance.now();renderFrame(frameState);const renderMs=performance.now()-renderStarted,frameMs=performance.now()-frameStarted,smooth=.08;perf.update+=smooth*(updateMs-perf.update);perf.render+=smooth*(renderMs-perf.render);perf.frame+=smooth*(frameMs-perf.frame);perf.fps+=smooth*((dt>0?1/dt:60)-perf.fps);drawPerf(frameState);raf=requestAnimationFrame(frame)};
    const keyDown=e=>{const k=e.key.toLowerCase();if(k==="f3"){e.preventDefault();perf.show=!perf.show;return}if(["w","a","s","d","arrowup","arrowdown","arrowleft","arrowright"," ","shift","e"].includes(k)){e.preventDefault();keys.add(k);if(k==="e")interact()}},keyUp=e=>keys.delete(e.key.toLowerCase()),pointerMove=e=>{mouse.x=(e.clientX-canvasRect.left)/canvasRect.width*W;mouse.y=(e.clientY-canvasRect.top)/canvasRect.height*H},pointerDown=e=>{if(e.button===0){e.preventDefault();mouse.down=true}},pointerUp=()=>mouse.down=false;
    const removeListeners=()=>{window.removeEventListener("keydown",keyDown);window.removeEventListener("keyup",keyUp);window.removeEventListener("pointerup",pointerUp);window.removeEventListener("resize",refreshCanvasRect);window.removeEventListener("orientationchange",refreshCanvasRect);canvas.removeEventListener("pointermove",pointerMove);canvas.removeEventListener("pointerdown",pointerDown)};
    window.addEventListener("keydown",keyDown);window.addEventListener("keyup",keyUp);window.addEventListener("pointerup",pointerUp);window.addEventListener("resize",refreshCanvasRect,{passive:true});window.addEventListener("orientationchange",refreshCanvasRect,{passive:true});canvas.addEventListener("pointermove",pointerMove,{passive:true});canvas.addEventListener("pointerdown",pointerDown);cleanup=()=>{ended=true;cancelAnimationFrame(raf);removeListeners();chunkCache.clear();glowCache.clear();particlePool.length=0};updateInventory();updateHud();discover("zones","upper");raf=requestAnimationFrame(frame);
  }
  renderTitle();
}

GubuntuGames.register("voidminer",startVoidMiner);
