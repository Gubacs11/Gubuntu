# Game modules

Each playable game owns one JavaScript file in this directory. The files are loaded before `app.js`, which provides the shared arcade shell, persistence, audio, input, and UI helpers.

`card-games-shared.js` contains the deck and Card Lounge helpers shared by Blackjack and Poker.
